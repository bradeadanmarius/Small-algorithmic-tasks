array = ["ion", 25, "carnati", "varza", "sarac", "lipit"]

for (index = 0; index <= (array.length - 1); index++) {
    if (index % 2 == 0) {
        //console.log(array[index])
    }
    console.log(array[index])
}